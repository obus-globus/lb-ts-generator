# Don't let the Kotlin Gradle plugin auto-add kotlin-stdlib as a bundled
# `implementation` dependency — we declare it `compileOnly` (see build.gradle) so
# the shadow jar ships NO Kotlin and uses LiquidBounce's runtime Kotlin instead.
kotlin.stdlib.default.dependency=false
